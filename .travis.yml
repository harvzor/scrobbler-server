language: generic
if: branch = master
git: { depth: 3 }
arch:
  # - amd64
  - arm64
os: linux
services:
  - docker
script:
  - REMOTE="harvzor"
  - LOCAL_IMAGE="scrobbler-server"
  - docker build -t $LOCAL_IMAGE .
deploy:
  - provider: script
    script: publish_images.sh
    skip_cleanup: true
